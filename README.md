This is learn react demo!

接口:   https://music.aityp.com
API:    https://binaryify.github.io/NeteaseCloudMusicApi/#/


1、建议对象遍历采用for...of，数组遍历采用for...in
正确答案：错误    

2、将数组成员赋值给变量时，使用数组解构	
正确答案：正确

3、如果通过 if 和 else 使用多行代码块，可以不把 else 放在 if 代码块关闭括号的同一行  
正确答案：错误 

4、需要回传多个值时，使用数组解构，而不是对象解构			
正确答案：错误

5、ES6的一些新特性使得编程更简洁，但跟以前的特性差异较大；使用ES6前JavaScript开发团队需对ES6的特性有一致的理解。		
正确答案：正确

6、创建有动态属性名的对象时，尽量在一个地方定义对象的所有属性		
正确答案：正确

7、长度超过50的字符串应该使用字符串连接换行				
正确答案：错误

8、在代码风格的规则中，当你导出单例、函数库、空对象时使用帕斯卡式命名
正确答案：正确 

9、不要使用 arguments。可以选择 rest 语法 ... 替代
正确答案：正确

10、在某些情况下需要违反JavaScript语言编程规范_ES6篇给出的规则时，个体程序员汇报申请即可。
正确答案：错误 

11、ES6发布的特性有些跟ES5原则和习惯有冲突，项目启用时团队需要决策项目依赖ES5还是ES6，并遵循相关规范。
正确答案：正确

12、在代码风格的规则中，当你导出默认的函数时使用驼峰式命名，你的文件名不一定要和函数名保持一致。
正确答案：错误

13、变量必须显式声明作用域
正确答案：正确

14、ES6不允许在对象之中，只写属性名，不写属性值
正确答案：错误

15、构建字符串时，使用字符串模板而不是字符串连接
正确答案：正确

16、关于数组的建议使用说法，错误的是
A、扩展运算符可以通过减少赋值语句的使用使代码更加简洁优雅，可读性更佳。
B、扩展运算符可以通过减少通过下标访问数组或对象的方式，使代码更加简洁优雅，可读性更佳。
C、使用扩展运算符 ... 复制数组
D、扩展运算符可以通过增加通过下标访问数组或对象的方式，加强代码的规范性和准确性
正确答案：D 

17、如下代码命名正确的是：
A、function getCustomerInfo()；
B、var firstname = ‘xiaoming‘;
C、function DRAW()；
D、var hotleGetUrl = ‘http://map.baidu.com/detail‘；
正确答案：A

18、关于箭头函数的使用规则，错误的是
A、别保存 this 的引用。使用箭头函数或 Function#bind。
B、当你必须使用函数表达式（或传递一个匿名函数）时，使用箭头函数符号
C、如果一个函数适合用一行写出并且只有一个参数，那就把花括号、圆括号和 return 都省略掉。
D、如果一个函数适合用一行写出，但是有多个参数，也可以把花括号、圆括号和 return 都省略掉。
正确答案：D

19、关于解构的规则要求，下面的说法错误的是
A、使用解构存取和使用多属性对象
B、将数组成员赋值给变量时，使用数组解构
C、解构赋值避免了临时变量或对象，给JS书写带来了很大的便利性，同时也提高了代码的可读性。
D、需要回传多个值时，使用数组解构。
正确答案：D

20、以下方法的命名中，哪个是符合规范的
A、function DRAW()
B、function visible()
C、function isFinished()
D、public void KeyListener(Listener)
正确答案：C

21、”use strict”对Javascript提出了更严格的语法，如果代码中使用"use strict"开启了严格模式,则下面哪种情况不会在脚本运行之前抛出SyntaxError异常
A、八进制语法:var n = 023和var s = "\047"
B、var t = 3
C、with语句
D、在语句块中使用函数声明：if(a<b){ function f(){} }
正确答案：B

22、关于变量的声明const，说法错误的是
A、用于声明块级域的局部只读变量；
B、与let相似，不同的是const声明的变量是只读的。
C、在ES6中，const代表一个值的常量索引
D、变量名字在内存中的指针可以改变，但是指向这个变量的值不可改变。
正确答案：D 

23、关于使用对象方法的简写的说法，错误的是
A、可以在对象字面量里面定义原型
B、定义方法可以不用function关键字
C、直接调用父类方法
D、不能直接调用父类方法
正确答案：D

24、下面关于代码风格的规则要求，哪个是错误的？
A、当你导出默认的函数时使用驼峰式命名。你的文件名必须和函数名完全保持一致。
B、当你导出默认的函数时使用驼峰式命名，你的文件名不用和函数名完全保持一致。
C、当你导出单例、函数库、空对象时使用帕斯卡式命名。
正确答案：B

25、关于函数的使用原则问题，错误的是
A、使用函数声明代替函数表达式
B、因为函数声明是可命名的，所以他们在调用栈中更容易被识别。
C、函数声明会把整个函数提升（hoisted）
D、函数表达式会把整个函数提升（hoisted）
正确答案：D

26、以下哪种操作，最节约性能
1）
var $list = $('#list');
for (var i = 0; i < 100; i++) {
    $list.append('<li>' + i + '</li>');
}
2）
var $list = $('#list');
var fragment = '';
for (var i = 0; i < 100; i++) {
    fragment += '<li>' + i + '</li>';
}
$list.append(fragment);
3）
var $list = $('#list');
for (var i = 0; i < 100; i++) {
    $list.appendTo('<li>' + i + '</li>');
}回答正确
A、1）
B、2）
C、3）
正确答案：B 

27、关于对象和引用的规则的说法，说法错误的是
A、创建有动态属性名的对象时，尽量在一个地方定义对象的所有属性
B、在一个地方定义对象的所有属性，增加代码的可读性。
C、ES6不允许在对象之中，只写属性名，不写属性值。
D、使用对象属性值的简写
正确答案：C

28、关于模块的使用规则问题，错误的是
A、代码中总是使用ES6标准的模块(import/export)方式， 而不是使用非标准的模块化加载器。
B、不要使用通配符 * 的 import
C、不要从 import 中直接 export
D、如果你的文件只输出一个类，那你的文件名和类名可以不用保持一致
正确答案：D

29、关于逗号的规则使用方法，错误的是
A、逗号写在行尾，并且增加结尾的逗号
B、这种行尾逗号，被称为“尾后逗号” （有时叫做“终止逗号”），在向 JavaScript 代码添加元素、参数、属性时十分有用。
C、在上一行已经使用了尾后逗号的情况下，如果要添加新的属性，在添加新的一行，且需要修改上一行。
D、逗号写在行尾，这使得版本控制的比较更加清晰，以及代码Merge的麻烦更少。
正确答案：C

30、控制一行的宽度，不要超过多少个字符
A、60
B、120
C、150
D、80
正确答案：B

31、建议数组遍历采用何种方式？
A、for...in
B、for...of
C、$.forEach()
正确答案：B 

32、关于函数的使用规则问题，错误的是
A、不要使用 arguments。可以选择 rest 语法 ... 替代。
B、rest语法参数是一个类数组。
C、直接给函数的参数指定默认值，不要使用一个变化的函数参数
D、直接给函数参数赋值时需要避免副作用。
正确答案：B

33、构建字符串时，下列哪种方式更加推荐？
A、function sayHi(name) {
    return 'How are you, ' + name + '?';
}
B、function sayHi(name) {
    return ['How are you, ', name, '?'].join();
}
C、function sayHi(name) {
    return `How are you, ${name}?`;
}
正确答案：C

34、长度超过多少字符串应该使用字符串连接换行
A、 80
B、 90
C、 100
D、 70
正确答案：A

35、关于分号的规则使用方法，错误的是
A、ECMAScript中的语句以一个分号结尾；如果分号省略，则由浏览器确定语句的结尾。
B、语句结尾的分号不是必须的，所以直接省略它
C、加上这个分号可以避免很多错误（例如不完整的输入），开发人员也可以放心地通过删除多余的空格来压缩ECMAScript代码（代码行结尾处没有分号会导致压缩错误）。
D、加上分号某些时候也会增进代码的性能，因为这样解析器就不用再花时间推测应该在哪里插入分号了。
正确答案：B 

36、下列关于存取器描述正确的是
A、属性的存取函数不是必须的，如果需要存取函数使用get方法和set方法
B、创建get()和set()函数不需要保持一致
C、dragon.age(25)这种是好的存取器编写方式
D、属性是布尔值，存取函数使用 isVal() 或 hasVal()。
正确答案：AD

37、以下说法错误的是 
A、页面局部刷新调整时，不需要注销相应的定时器和延时器
B、可以在JS代码中大量使用console.log输出
C、数组使用完后可以不将数组长度置空
D、直接将Javascript字符串作为DOM的事件处理逻辑，容易引发IE8版本下无法释放内存问题
正确答案：ABC 

38、下列有关箭头函数的描述正确的是
A、别保存 this 的引用。使用箭头函数或 Function#bind
B、可以保存 this 的引用。使用箭头函数或 Function#bind
C、当你必须使用函数表达式时，使用箭头函数符号
D、当你必须使用传递一个匿名函数时，使用箭头函数符号
正确答案：ACD

39、对于ES6需要遵守的代码原则，哪些是正确的
A、清晰第一
B、简洁为美
C、选择合适的风格和标准，团队保持一致
D、ES6的一些新特性使得编程更简洁，但跟以前的特性差异较大；使用ES6前JavaScript开发团队需对ES6的特性有一致的理解。
E、安全高效
正确答案：ABCDE   

40、下列关于类型和变量描述正确的是
A、尽量对所有的引用使用const，不要使用 var；如果你一定需要可变动的引用，使用 let 代替 var
B、在需要的地方给变量赋值，但请把它们放在一个合理的位置
C、将所有的 const 和 let 分组
D、const和let的作用域更小，写代码更容易控制
正确答案：ABCD

41、变量的声明包含三种，请选择
A、var
B、let
C、const
D、import
正确答案：ABC 

42、使用对象方法的简写表现在哪些方面？
A、可以在对象字面量里面定义原型
B、定义方法可以不用function关键字
C、直接调用父类方法
D、定义方法必须用function关键字
正确答案：ABC

43、关于逗号的使用方法，下面的说法正确的是
A、行尾逗号，被称为“尾后逗号” （有时叫做“终止逗号”），在向 JavaScript 代码添加元素、参数、属性时十分有用。
B、在上一行已经使用了尾后逗号的情况下，如果要添加新的属性，可以仅仅添加新的一行，而不需要修改上一行。
C、在上一行已经使用了尾后逗号的情况下，如果要添加新的属性，需要修改上一行。
D、行尾逗号，使得版本控制的比较更加清晰，以及代码Merge的麻烦更少
正确答案：ABD  

44、下列有关字符串描述正确的是
A、长度超过120的字符串应该使用字符串连接换行
B、字符串超长影响可读性
C、过度使用字串连接符号可能会对性能造成影响
D、长度超过80的字符串应该使用字符串连接换行
正确答案：BCD

45、对于扩展运算符的说法，正确的是
A、扩展运算符可以通过减少赋值语句的使用使代码更加简洁优雅，可读性更佳。
B、扩展运算符可以通过减少通过下标访问数组或对象的方式，使代码更加简洁优雅，可读性更佳。
C、扩展运算符可以通过增加赋值语句的使用，加强代码的规范性和准确性。
D、扩展运算符可以通过增加通过下标访问数组或对象的方式，加强代码的规范性和准确性。
正确答案：AB 

46、下列有关模块的描述正确的是
A、代码中总是使用ES6标准的模块(import/export)方式， 而不是使用非标准的模块化加载器
B、不要使用通配符 * 的 import
C、不要从 import 中直接 export
D、如果你的文件只输出一个类，那你的文件名必须和类名完全保持一致
正确答案：ABCD 

47、下面说法正确的是
A、使用 . 来访问对象的属性
B、属性是动态的时候使用 []
C、使用[]来访问对象的属性
D、属性是动态的时候使用 . 
正确答案：AB 

48、对于ES6中的使用对象方法和使用对象属性的简写的描述，下面的说法正确的是：
A、可以在对象字面量里面定义原型
B、定义方法可以不用function关键字
C、直接调用父类方法
D、ES6 要求在对象之中，属性名和属性值都必须写
正确答案：ABC

49、关于事件监听说法正确的是
A、正确使用DOM Event Level 2/3标准进行事件的绑定和注销
B、采用非标准的事件绑定方式容易受浏览器的特殊实现的影响，造成内存无法释问题
C、页面卸载DOM对象时不需要注销绑定的事件监听
D、jQuery组件会自动选择浏览器支持的最高事件模型来绑定事件和注销事件。
正确答案：ABD

50、下列有关分号的使用描述正确的是？
A、 ECMAScript中的语句以一个分号结尾
B、如果分号省略，则由浏览器确定语句的结尾
C、虽然语句结尾的分号不是必须的，但是建议任何时候都不要省略它
D、 ECMAScript中的语句以一个逗号结尾
正确答案：ABC






--安全--

1、注释中可以含有工号信息、姓名、部门、邮箱等信息。
正确答案：错误

2、禁止封装基本类型
正确答案：正确

3、未验证的输入作为重定向URL会发生Open redirect漏洞
正确答案：正确 

4、功能失效时必须彻底删除对应的功能代码。
正确答案：正确 

5、对于入站身份验证，可直接对口令采用md5哈希算法进行计算，并且在有适当的访问控制的配置文件或数据库中存储hashes。
正确答案：错误

6、在访问某个外部对象或外部对象内的方法及属性时，只需判断该对象是否为空。
正确答案：错误 

7、函数块内必须使用函数表达式声明函数
正确答案：正确

8、禁止直接将不可信数据插入到WEB页面中
正确答案：正确

9、在代码外存储口令，存储在一个强保护的加密的配置文件或数据库中，免受所有外部访问，包括在同一系统上的其他本地用户。
正确答案：正确

10、硬编码的口令会导致口令敏感信息泄露
正确答案：正确 

11、禁止使用with{}
正确答案：正确

12、声明变量时必须加上var、let、const关键字，否则可能会造成信息泄露问题
正确答案：正确

13、下面函数的声明是否符合JavaScript语言规范function bar(name) {
  if (name === "hotel") {
    function foo() {console.log("hotel foo A");}
   } else {
    function foo() {console.log("hotel foo 2");} 
 }
   foo();
  }
 bar("hotel");
正确答案：错误   

14、this仅可在对象构造函数、方法、闭包中使用
正确答案：正确 

15、声明变量时必须加上var关键字
正确答案：正确

16、下列说法与规范要求不一致的是
A、函数块内必须使用函数表达式声明函数
B、支持块内声明函数属于 ECMAScript 规范
C、内部声明的变量或者函数都是在其父函数或者脚本的作用域中，导致块内函数或者变量的声明会存在覆盖现象
D、如果确实需要在块中定义函数, 应该使用函数表达式来初始化。
正确答案：B 

17、对不可信的JS对象进行序列化时，不需要过滤的特殊字符有：
A、&
B、;
C、"
D、/
正确答案：B

18、下列特殊字符与编码结果匹配错误的是
A、& &
B、< "
C、> >
D、/ /
正确答案：B

19、声明变量时不加上var关键字会引发什么问题
A、定义变量会暴露到window上
B、GC可以正常回收内存
C、变量不会暴露在全局作用域（window）中
D、推荐声明变量不加上var关键字
正确答案：A 

20、假设访问脚本来自http://www.example.com/dir/page.html，以下哪个属于同源访问？
A、http://www.example.com/ other1/other2/index.html
B、http://www.example.com
:8080/dir/page.html 
C、http://www2.example.com/ dir/page.html
D、http://otherdomain.com
正确答案：A

21、以下JavaScript语言规范正确的是
A、可以使用IE下的条件注释
B、可以使用with{}
C、禁止封装基本类型
D、this可以在任意地方中使用
正确答案：C 

22、JavaScript源代码的混淆不包括
A、删除代码行中的空格和制表符
B、添加错误的注释信息
C、连接所有代码行
D、对前期阶段的结果进行编码
正确答案：B

23、在访问某个外部对象或外部对象内的方法及属性时时说法错误的是
A、必须先判断该对象是否为空
B、必须先判断该对象是否包含调用的方法
C、必须先判断该对象是否包含调用的属性
D、必须限制访问的时间
正确答案：D 

24、下列关于敏感信息的描述错误的是
A、禁止在 localStorage存储敏感信息
B、硬编码的口令将口令敏感信息泄露
C、可以在 localStorage存储敏感信息
D、禁止使用console.log()输出敏感信息
正确答案：C

25、将不可信数据组装成JSON对象 ，不需要过滤的特殊字符有：
A、\
B、&
C、"
D、<
正确答案：A 

26、下列说法与规范要求不一致的是
A、this可在对象构造函数、方法中使用。
B、在JavaScript里面，this指针代表的是执行当前代码的对象的所有者。
C、this指向的对象明确，不容易出错。
D、this可在闭包中使用。
正确答案：C  

27、在以下哪种情况下使用this是错误的
A、对象构造函数
B、对象方法
C、闭包
D、所有方法
正确答案：D

28、下列关于不可信数据描述错误的是
A、禁止直接将不可信数据插入到WEB页面中
B、可以直接对不可信的JS对象进行序列化
C、禁止使用eval()函数来处理来自外部的不可信数据
D、禁止直接将不可信数据组装成JSON对象
正确答案：B

29、下列说法与规范要求不一致的是
A、AngularJS中的表达式{{expression}}不能用来处理来自外部的不可信数据
B、禁止使用eval()函数来处理来自外部的不可信数据
C、eval()函数存在安全隐患，该函数可以把输入的字符串当作JavaScript表达式执行，容易被恶意用户利用
D、用户输入可以直接作为eval函数参数进行拼接的一部分
正确答案：D 

30、下列说法与规范要求不一致的是
A、禁止直接将不可信数据组装成JSON对象 
B、开发人员经常利用Ajax通过JSON方式来传递必要的信息给DOM，但是如果没有对用于构建JSON结构的来自外部的数据进行校验，有可能发生JSON注入
C、将不可信数据组装成JSON对象时应该验证JSON对象数据并记录验证结果。
D、将不可信数据组装成JSON对象时应该过滤某些特殊字符。
正确答案：C  

31、验证不可信JS数组元素，不需要过滤的特殊字符有：
A、&
B、`
C、<
D、>
正确答案：B 

32、下列说法与规范要求不一致的是
A、声明变量时可以加上var关键字也可以不加
B、不使用var声明变量，变量就会暴露在全局作用域（window）中。
C、不使用var声明变量可能会覆盖全局作用域中的同名变量，从而引发GC无法有效回收内存问题。
D、当变量中包含敏感信息时，暴露到全局作用域可能会导致信息泄露问题。
正确答案：A 

33、下列说法与规范要求不一致的是
A、禁止未验证的输入作为重定向URL
B、禁止将未验证的输入作为客户端系统路径
C、禁止直接将不可信数据插入到WEB页面中
D、禁止将不可信数据组装成JSON对象 
正确答案：D

34、功能失效时说法正确的是
A、必须彻底删除相应的代码
B、可用使用注释行的形式处理，方便后续使用
C、可用使用注释块的形式处理
D、可用放到不用的函数中
正确答案：A 

35、以下哪种数据类型不是JavaScript中的基本数据类型
A、Object
B、String
C、Undefined
D、Number
正确答案：A 

36、关于将不可信数据插入到WEB页面中的描述与规范一致的是
A、在把HTML和JavaScript插入执行环境上下文的HTML当中之前，先把它们进行转义
B、  在插入不可信数据到执行环境中的HTML 属性上下文之前，先转义JavaScript。
C、当插入不可信数据到事件处理程序和执行环境中javaScript上下文中的时，要先转义JavaScript。
D、 在插入不可信数据到执行环境的CSS属性上下文之前，转义CSS。
正确答案：ABCD 

37、JavaScript源代码的混淆包括
A、 使用无意义符号更换符号名称。
B、 使用表达式替换数字常量。
C、 使用十六进制转义，更换字符串。
D、 删除或混淆意见
正确答案：ABCD 

38、JavaScript源代码的混淆包括以下哪几项
A、删除代码行中的空格和制表符
B、对前期阶段的结果进行编码
C、使用无意义符号更换符号名称。
D、使用十六进制转义，更换字符串。例如"cust"转换成"\x63\x75\x73\x74"
正确答案：ABCD

39、注释中不能包含以下那些信息？
A、工号
B、姓名
C、部门
D、邮箱
正确答案：ABCD 

40、下列关于通用规则描述正确的是
A、访问外部对象时，需先判断该对象是否为空
B、未进行判断，很有可能会访问了undefined的对象或不存在的方法及属性
C、JavaScript为解释性语言，若代码中的功能失效时必须彻底删除相应的代码
D、带有员工个人信息的注释有可能会泄露具体的开发人员信息，从而引入社会工程学方面的风险
正确答案：ABCD

41、下列关于用户口令说法正确的是
A、硬编码的口令将口令敏感信息泄露
B、 对于出站身份验证：在代码外存储口令，存储在一个强保护的加密的配置文件或数据库中，免受所有外部访问，包括在同一系统上的其他本地用户。妥善保护密匙。如果你不能使用加密以保护文件，那么请确保尽可能严格的限制权限。
C、比起对第一次登录的默认用户名和口令进行硬编码，利用"first login" （第一次登录）模式，要求用户输入唯一的强口令是更好的方法。
D、使用随机分配salts（盐）为您生成每个单独的hash。这增加了攻击者需要进行暴力攻击的计算量，可能会限制彩虹表方法的有效性。
正确答案：ABCD 

42、下列有关类的定义描述正确的是
A、定义类时，应在原型下定义方法
B、定义类时，应在原型下定义属性
C、定义类时，在构造方法内定义属性
D、定义类时，应在构造方法内定义方法
正确答案：AC

43、下列关于HTML5映射描述正确的是
A、sessionStorage 映射仅在页面实例和即时浏览器会话期间为调用页面提供存储
B、禁止在 sessionStorag存储敏感信息
C、localStorage 映射会提供可供多个页面实例和浏览器实例访问的存储
D、使用原型prototype实现继承时，尽量使用现有稳定的库方法而非自行实现
正确答案：AC

44、下列说法与规范要求一致的有
A、禁止使用IE下的条件注释
B、禁止修改内置对象的原型
C、在使用原型prototype实现继承时，尽量自行实现
D、定义类时，应在原型下定义方法，在构造方法内定义属性
正确答案：ABD

45、下列有关输入校验描述正确的是
A、可以使用eval()函数来处理来自外部的不可信数据
B、禁止直接对不可信的JS对象进行序列化
C、禁止直接将不可信数据组装成JSON对象 
D、禁止未验证的输入作为重定向URL
正确答案：BCD 

46、禁止封装类型有：
A、Undefined
B、Null
C、Boolean
D、Number
正确答案：ABCD 

47、下列说法与规范要求一致的有
A、 安全敏感对象不应被序列化。
B、验证反序列化对象的数据，过滤附录A中的特殊字符。
C、禁止直接对不可信的JS对象进行序列化
D、一个JS对象可以同时拥有数据和方法。如果这些数据或者方法来自用户输入，那么这个对象的序列化将产生可以被注入代码利用的安全漏洞。
正确答案：ABCD

48、下列关于注释信息说法正确的是
A、不能够体现个人工号信息
B、不能够体现个人姓名
C、不能够体现个人部门
D、不能够体现个人邮箱
正确答案：ABCD

49、在IE下使用以下哪些语句可以激活条件编译？
A、@cc_on
B、@if
C、@set
D、@fun
正确答案：ABC  

50、下列有关硬编码用户口令的解决方案哪些是正确的
A、 对于出站身份验证：在代码外存储口令，存储在一个强保护的加密的配置文件或数据库中
B、 比起对第一次登录的默认用户名和口令进行硬编码，利用"first login" （第一次登录）模式，要求用户输入唯一的强口令是更好的方法
C、 对于入站身份验证：对口令采用强大的单向hashes（散列）并且在有适当的访问控制的配置文件或数据库中存储hashes
D、使用随机分配salts（盐）为您生成每个单独的hash。这增加了攻击者需要进行暴力攻击的计算量，可能会限制彩虹表方法的有效性
正确答案：ABCD 
